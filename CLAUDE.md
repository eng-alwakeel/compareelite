# CompareElite — Project Instructions

## Report Format

Every report (health checks, analytics, summaries, audit results) must be output inside a **single fenced code block** so the user can copy it directly. No report content outside the code block.
